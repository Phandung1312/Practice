#include <bits/stdc++.h>
// #include "../../debug.h"

using namespace std;
#define int long long

void process(vector<vector<int, int>>& vector, int n){
    
}

int32_t main(){
    int t;
    cin >> t;
    while(t--){
        int n;
        cin >> n;
        vector<vector<int>> a(n, vector<int>(n));
        for(int i = 0; i < n; i++){
            for(int j = 0; i < j; j++){
                cin >> a[i][j];
            }
        }
    }
    return 0;
}